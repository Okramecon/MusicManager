@model IEnumerable<MusicManager.ViewModels.UserViewModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Пользователи</h1>

@*<p>
    <a asp-action="CreateAdmin">Добавить админа</a>
</p>*@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoleName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoleName)
            </td>
            <td>
                <a asp-action="UserPlaylists" asp-controller="Playlists" asp-route-userId="@item.Id">Плейлисты</a>
                @*<a asp-action="Edit" asp-route-id="@item.Id">Изменить</a> |*@
                @*@if (item.IsBlocked)
                {
                    <a asp-action="UnBlock" asp-route-id="@item.Id">Разблокировать</a>
                }
                else
                {
                    <a asp-action="Block" asp-route-id="@item.Id">Заблокировать</a>
                }*@
            </td>
        </tr>
}
    </tbody>
</table>
